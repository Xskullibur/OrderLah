<div style="position: fixed; right: 3vw; bottom: 3vh;">

    <button id="bottom_btn" type="button" class="btn mdc-fab btn-circle mr-3 mb-3">
        {{#if icon}}
            <i class="{{icon}}"></i>
        {{else}}
            <i class="fas fa-angle-double-up icon-default"></i>
        {{/if}}
    </button>


</div>